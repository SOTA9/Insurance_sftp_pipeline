# Airflow
apache-airflow==2.8.4
apache-airflow-providers-google==10.14.0
apache-airflow-providers-slack==8.5.0

# Transport & cryptography
paramiko==3.4.0
python-gnupg==0.5.2

# Config — REQUIRED by pipeline/directory_config.py (yaml.safe_load),
# keys/generate_pgp_keypair.py --generate-all, and docker/entrypoint.sh's
# Python block. Was missing — every multi-directory code path imports it.
pyyaml==6.0.1

# GCP
google-cloud-storage==2.16.0
google-cloud-bigquery==3.22.0
google-cloud-secret-manager==2.20.0   # production secret fetching

# Validation
pandera[io]==0.19.3
pydantic-settings==2.2.1
pandas==2.2.2

# HTTP
requests==2.31.0

# Testing
pytest==8.1.1
pytest-mock==3.14.0
pytest-cov==5.0.0
moto[s3]==5.0.3